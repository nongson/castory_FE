export default {
  handleGetCardSet(context, payload) {
    // handle call API
    context.commit("handleGetCardSet", payload);
  },

  handleGetCardSetDetails(context, payload) {
    // handle call API
    context.commit("handleGetCardSetDetails", payload);
  },
};
